How to donwload/build/run:

Clone git repository (or download as .zip and extract)

Navigate to each question's related folder, and run...

On Linux/Unix/Mac:
`./gradle run`

On Windows:
`./gradlew.bat run`
